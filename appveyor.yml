version: 1.0.{build}
configuration: Release
platform: Any CPU
before_build:
- cmd: dotnet restore
build:
  project: HereDotNet.sln
  verbosity: minimal
after_test:
- cmd: dotnet pack -p:PackageVersion=%APPVEYOR_BUILD_VERSION%
artifacts:
- path: '**/*.nupkg'
  name: Packages
deploy_script:
- cmd: dotnet nuget push "**/*.nupkg" --api-key oy2b6y7adalmi3uu2nvatmfwpoxnauluvgtgwwfci4oevu --source https://api.nuget.org/v3/index.json
